# S36 plan — close hallucination_score → 6/6 ship-gate

**Date:** 2026-05-18
**Premise:** the N=30 verification (#103) confirmed S35 worked. The trade
oracle is now **5/6 statistically locked** (up from 3/6). One dimension blocks
a clean 6/6 ship-gate: `hallucination_score`.

## Where the ship-gate stands (#103, clean N=30)

| Dimension | locked? |
|---|---|
| verdict_accuracy 1.000 | 🔒 |
| citation_relevance 0.703 | 🔒 (was 0.468 — the S35 envelope split) |
| provider_kind_coverage 1.000 | 🔒 |
| dissent_grounding 0.637 | 🔒 |
| confidence_calibration 0.630 | 🔒 |
| **hallucination_score 0.367** | ❌ CI [0.20,0.53] — won't lock |

## The blocker — two parts

`hallucination_score` mean (0.367) clears the 0.30 threshold, but its 95% CI
does not. Two distinct problems:

1. **Product.** The panel emits an ungrounded numeric figure on ~6/10
   fixtures. S29-#33's "quote-or-disclose" prompt addendum and S31-#49's hall
   post-processing validator are not binding. Needs a **code** enforcement.
2. **Measurement.** `hallucination_score` is a binary dimension with
   std ≈ 0.49 (near-maximal for a binary). N=30 cannot statistically certify
   it even when the mean is fine — confirmed by the quant read on #103. It
   needs **N ≥ 50** to lock.

## Workstreams

| WS | Scope | Owner |
|---|---|---|
| **WS1** | Diagnose the ~6/10 ungrounded-figure rate from the #103 artifacts — which fixtures, what figures, why #33 + #49 miss them. Free/read-only. | ai-ml-engineer |
| **WS2** | Grounding-or-abstain enforcement — every numeric claim quotes a retrieved chunk or the panel abstains. Code, not prompt. | ai-ml-engineer |
| **WS3** | The hallucination re-measure runs at **N ≥ 50** (binary dimension — N=30 can't certify). Founder-gated spend. | quant-analyst |
| #104 | `make test-fast` OOM-kills — full-suite verification is impossible. Important infra debt. NOT an overnight dispatch (an agent running the OOMing suite could freeze the box). | software/devops |

## Carry-over (S35 tail)

- #91 — canon_berkshire snippet mojibake (PDF-extraction failure).
- #92 — protocol_native pulls wrong-instrument data (drift JTO → ETH-PERP).
- #100 — pooling contract test (every consumer routes through `load_poolable_rows`).
- Cross-repo: `skill.md` (gecko-claude) + demo (gecko-mcpay-app) need the
  two-field envelope update — handoff written 2026-05-18.

## Sequencing

WS1 (diagnosis, free) fires first → WS2 (the fix) → an N≥50 re-measure
confirms 6/6 (founder-gated — that's real spend, not auto-fired). #104 is
supervised work, not overnight. The 6/6 ship-gate is reachable: 5 dimensions
already hold; this is the last one.
