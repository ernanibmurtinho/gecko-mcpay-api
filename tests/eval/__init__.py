"""Pro tier eval harness — 20 fixed ideas, rubric scoring, baseline diffs.

Default mode is mock (no network, no $). Use `--live` to invoke the real
5-agent debate via `gecko_core.orchestration.pro.generate`.
"""
