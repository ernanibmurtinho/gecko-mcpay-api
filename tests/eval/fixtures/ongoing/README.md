# ongoing fixtures

Operational-tier suites for S15+ (post-launch monitoring, ongoing
research delta). Empty in S13.

When S15 lands its first ongoing-phase fixture, drop it here. The loader
in `tests/eval/runner.py` already routes `phase='ongoing'` to this
directory.
